# Sprint 1 SB1-08/09/13/14 完了チェック

## 1. 対象

| SB-ID | 項目 | 判定 |
| --- | --- | --- |
| SB1-08 | FastAPIの基本構成を実装する | 完了 |
| SB1-09 | 疎通確認用APIを実装する | 完了 |
| SB1-13 | DB接続設定を実装する | 完了 |
| SB1-14 | DB接続確認を実施する | 完了 |

## 2. 確認日

- 確認日: 2026-04-21
- 対象環境: local Windows + Docker Desktop + standard構成

## 3. 完了条件チェック

| 観点 | 確認内容 | 結果 |
| --- | --- | --- |
| FastAPI起動 | `standard-api` コンテナが起動し、healthcheckがhealthyになること | OK |
| API疎通 | `GET /api/v1/health` が `success` を返すこと | OK |
| DB起動 | `standard-db` コンテナが起動し、PostgreSQL healthcheckがhealthyになること | OK |
| DB初期化 | `001_standard_schema.sql` が実行され、`app_metadata` が作成されること | OK |
| DB疎通 | `GET /api/v1/health/db` が `SELECT 1` 相当の確認結果を返すこと | OK |
| WebUI連携 | HOME画面から `GET /api/v1/health` を呼び、API疎通状態を表示すること | OK |
| 自動テスト | backend pytestが成功すること | OK |
| frontend build | WebUI buildが成功すること | OK |

## 4. 実行コマンド

```powershell
docker compose -f docker-compose.yml -f docker-compose.standard.yml up -d --build standard-db standard-api
```

```powershell
Invoke-RestMethod http://localhost:8000/api/v1/health
Invoke-RestMethod http://localhost:8000/api/v1/health/db
```

```powershell
cd apps\standard\backend
.\.venv\Scripts\python.exe -m pytest
```

```powershell
cd apps\standard\frontend
npm run build
```

## 5. 確認結果

API疎通確認:

```text
result: success
service: standard-api
environment: standard
message: API is available.
```

DB疎通確認:

```text
result: success
database: mvp_standard
host: standard-db
port: 5432
message: Database connection is available.
```

テスト:

```text
12 passed
```

frontend build:

```text
vite build success
```

## 6. 補足

- Windows側で `127.0.0.1:8000` に古いローカルuvicornのポート予約が残っていたため、Docker公開ポートの確認には `localhost:8000` を使用した
- WebUIのAPI接続先も `http://localhost:8000` を標準値とした
- Docker Desktopが未起動の場合は、先にDocker Desktopを起動してからComposeを実行する
